import styled from "styled-components";

const CurationFooter = () => {
    return (
        <CurationFooterStyle>
            © 글에 대한 저작권은 식식물물이 보유하고 있습니다.
        </CurationFooterStyle>
    )
}

export default CurationFooter;

const CurationFooterStyle = styled.div``;