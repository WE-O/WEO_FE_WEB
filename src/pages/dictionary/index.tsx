import { useAppSelector } from "../../store/hooks";
import { useEffect } from "react";
import DictionaryContainer from "../../components/Dictionary";

const Dictionary = () => {
    return (
        <DictionaryContainer/>
    );
};

export default Dictionary;