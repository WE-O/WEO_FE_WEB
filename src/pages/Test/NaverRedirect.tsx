import { useState } from "react"

type AppProps = {
    name: string
}

const NaverRedirect: React.FC<AppProps> = (props) => {

    return (
        <div>
            NaverRedirect!
        </div>
    )
}

export default NaverRedirect;